<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member"><!-- 인터페이스자리 -->
	<select id="login"
		parameterType="com.movie.ace.member.MemberVo"
		resultType="int">
	select count(*) from member where member_id = #{memberId} and movie_pw = #{memberPw}
	</select>
	 
</mapper>